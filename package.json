{
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "build": "cd mobile && npm install && npx expo export -p web"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "mongoose": "^9.7.0"
  }
}
